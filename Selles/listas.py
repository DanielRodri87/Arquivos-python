alunos = []
for x in range(10):
    alunos.append(input('Digite os nomes dos 10 alunos:'))
alunos.upper(alunos)
print(f'O primeiro nome da chamada é: {sorted(alunos)}')

# Quando apenas assistimos só aprendemos que o professor sabe programar, e não como fazer